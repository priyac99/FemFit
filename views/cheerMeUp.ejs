<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <% for(let i = 0;i<posts.length;i++){ %>

            <div>
                <h2><%=posts[i].title%></h2>
                <p><%=posts[i].body%></p>
                <%if(posts[i].link!=""){%>
                <a href="<%=posts[i].link%>">Click Here</a>
                <%}%>
               
            </div>
    
      

        <%}%>
</body>
</html> <% for(let i = 0;i<posts.length;i++){ %>

    <div>
        <div>
            <h2><%=posts[i].title%></h2>
            <p><%=posts[i].body%></p>
            <%if(posts[i].link!=""){%>
            <a href="<%=posts[i].link%>">Click Here</a>
            <%}%>
            <form action="/api/reviewAdd" method="post">
                <input type="hidden" name="id" value="<%=posts[i]['_id']%>">
                <input type="submit" value="Accept">
            </form>

            <form action="/api/reviewDelete" method="post">
                <input type="hidden" name="id" value="<%=posts[i]['_id']%>">
                <input type="submit" value="Discard">
            </form>
        </div>
    </div>
  

    <%}%>